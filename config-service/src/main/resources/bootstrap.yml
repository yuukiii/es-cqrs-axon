spring:
  profiles:
    active: dev
  application:
    name: config-service
  cloud:
    config:
      server:
        git:
          uri: https://github.com/ryukato/msa-sample-config
server:
  port: ${PORT:8888}

