spring:
  application:
    name: config-service
  cloud:
    config:
      server:
        git:
          uri: https://github.com/ryukato/msa-sample-config.git
server:
  port: ${PORT:8888}

logging:
    level:
        root: INFO
        org.springframework: DEBUG
        org.axonframework: INFO
        app: INFO